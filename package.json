{
  "name": "lynxtron-show-cases",
  "version": "0.0.1",
  "scripts": {
    "build": "pnpm -r --filter=!@lynxtron-examples/codex-demo run build",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "pnpm build && changeset publish --no-git-tag",
    "generate-registry": "node --experimental-strip-types scripts/generate-registry.ts",
    "local-registry": "node scripts/preview.mjs",
    "local-registry:build": "node scripts/preview.mjs --no-launch",
    "preview": "node scripts/preview.mjs",
    "preview:build": "node scripts/preview.mjs --no-launch"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.0",
    "app-builder-lib": "26.13.0",
    "dmg-builder": "26.13.0",
    "electron-builder-squirrel-windows": "26.13.0",
    "verdaccio": "6.3.2"
  },
  "engines": {
    "node": ">=22"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@lynx-js/lynxtron",
      "@lynx-js/lynxtron-builder",
      "better-sqlite3"
    ],
    "patchedDependencies": {
      "@lynx-js/lynxtron-rebuild@0.0.1-alpha.41": "patches/@lynx-js__lynxtron-rebuild@0.0.1-alpha.41.patch"
    }
  },
  "packageManager": "pnpm@10.15.1"
}
