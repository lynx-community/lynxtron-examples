{
  "name": "@lynxtron-examples/cross-platform-notes",
  "version": "0.0.1",
  "main": "main.js",
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/lynx-community/lynxtron-examples"
  },
  "scripts": {
    "build": "cross-env TARGET_ENV=desktop rspeedy build && rspack build",
    "build:web": "cross-env TARGET_ENV=web rspeedy build && cross-env TARGET_ENV=web rspack build",
    "dev": "cross-env TARGET_ENV=desktop NODE_ENV=development concurrently -k --raw \"rspeedy dev\" \"dev-ready-rspeedy && rspack dev\"",
    "dev:web": "cross-env TARGET_ENV=web NODE_ENV=development concurrently -k --raw \"rspeedy dev\" \"rspack serve\"",
    "serve:web": "node ./scripts/serve-web.mjs ./dist/web",
    "start": "cross-env TARGET_ENV=desktop npm run build && lynxtron ./dist/desktop",
    "start:web": "npm run build:web && npm run serve:web"
  },
  "showcase": {
    "description": "Cross-Platform Notes showcase scaffold with a shared Lynx UI for desktop and web",
    "thumbnail": "thumbnail.png",
    "targets": [
      "desktop",
      "web"
    ],
    "tags": [
      "cross-platform",
      "notes",
      "web"
    ],
    "minToolchainVersion": "0.0.1"
  },
  "dependencies": {
    "@lynx-js/lynxtron": "catalog:"
  },
  "devDependencies": {
    "@lynx-js/lynxtron-dev-plugins": "catalog:",
    "@lynx-js/react": "catalog:",
    "@lynx-js/rspeedy": "catalog:",
    "@lynx-js/types": "catalog:",
    "@lynxtron-showcases/config": "workspace:*",
    "@rspack/cli": "catalog:",
    "@rspack/core": "catalog:",
    "concurrently": "catalog:",
    "cross-env": "catalog:",
    "typescript": "catalog:"
  }
}
