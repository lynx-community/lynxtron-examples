---
"lynxtron-go": patch
---

Automatically publish Go installers after Changesets versioning, using one package version and pinned source commit for the release tag, installers and showcase assets. Reject mismatched tags and publish only after both platforms build successfully.
