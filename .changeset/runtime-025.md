---
"lynxtron-go": patch
---

Upgrade the bundled Lynxtron runtime and coordinated CEF, native headers and build tooling from 0.0.23 to 0.0.25. Rebuild native extensions against the new runtime, including upstream Windows CEF OSR and macOS surface lifetime fixes. Release a new Go compatibility lane rather than changing the runtime in an existing showcase channel.
