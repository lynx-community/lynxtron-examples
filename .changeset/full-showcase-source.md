---
"lynxtron-go": patch
---

Remove depth, file count and file size truncation from the showcase source tree. Keep build outputs excluded and avoid following symlinks; limit only the initially visible editor panes.
