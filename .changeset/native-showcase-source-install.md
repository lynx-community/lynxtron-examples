---
"lynxtron-go": patch
"@lynxtron-examples/native-texture-canvas": patch
"@lynxtron-examples/electron-fiddles": patch
---

Display native source and build definitions in Go. Bundle local showcase runtime dependencies into release archives so isolated npm and pnpm consumers can install them without workspace overrides.
