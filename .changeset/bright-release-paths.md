---
'@lynxtron-examples/cli': patch
'lynxtron-go': patch
---

Build release installers with packed showcase asset URLs and the application version, and install source-only showcase fallbacks with npm including devDependencies so build commands can resolve executable dependencies such as cross-env.
