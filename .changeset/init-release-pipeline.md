---
"@lynxtron-examples/config": patch
---

Set up the release pipeline: publish the shared build config to npm via
Changesets + npm OIDC trusted publishing, and build Lynxtron GO installers and
showcase tarballs as GitHub Release assets.
